# name: Deploy MkDocs on PR Close

# on:
#   pull_request:
#     types: [closed]

# permissions:
#   contents: write

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Get latest successful artifact
#         id: get_artifact
#         uses: actions/github-script@v6
#         with:
#           script: |
#             const { owner, repo } = context.repo;
#             const workflow_id = 'github-actions.yml'; // Nom du workflow à surveiller

#             const runs = await github.rest.actions.listWorkflowRuns({
#               owner,
#               repo,
#               workflow_id: workflow_id,
#               status: "success",
#               per_page: 1
#             });

#             if (!runs.data.workflow_runs.length) {
#               core.setFailed("Aucun run réussi trouvé.");
#               return;
#             }

#             const run_id = runs.data.workflow_runs[0].id;

#             const artifacts = await github.rest.actions.listWorkflowRunArtifacts({
#               owner,
#               repo,
#               run_id: run_id
#             });

#             const artifact = artifacts.data.artifacts.find(a => a.name === "github-pages");

#             if (artifact) {
#               console.log(`✅ Artifact trouvé: ID ${artifact.id}`);
#               core.setOutput("artifact_id", artifact.id);
#             } else {
#               core.setFailed("❌ Aucun artifact 'github-pages' trouvé.");
#             }

#       - name: Download and extract artifact
#         if: success()
#         env:
#           ARTIFACT_ID: ${{ steps.get_artifact.outputs.artifact_id }}
#         run: |
#           mkdir -p /tmp/gh-artifact-extract
#           curl -L -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
#             -H "Accept: application/vnd.github+json" \
#             "https://api.github.com/repos/${{ github.repository }}/actions/artifacts/$ARTIFACT_ID/zip" \
#             -o artifact.zip

#           unzip artifact.zip -d /tmp/gh-artifact-extract
#           mkdir -p docs/site
#           tar -xvf /tmp/gh-artifact-extract/artifact.tar -C docs/site/

#       - name: Remove PR branch directory
#         run: |
#           BRANCH_NAME="${{ github.event.pull_request.head.ref }}"
#           rm -rf "docs/site/$BRANCH_NAME"
#           echo "🗑️ Dossier supprimé: docs/site/$BRANCH_NAME"

#       - name: Deploy artifact
#         run: |
#           echo "🚀 Déploiement du site MkDocs"
#           git config --global user.name "github-actions[bot]"
#           git config --global user.email "github-actions@github.com"
#           git add docs/site
#           git commit -m "🛠️ Mise à jour du site après la fermeture de la PR #${{ github.event.pull_request.number }}"
#           git push
